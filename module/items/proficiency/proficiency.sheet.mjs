import { MyttItemSheet } from '../item-sheet.mjs'

export class ProficiencySheet extends MyttItemSheet {
  getData() {
    const context = super.getData();

    return context;
  }


  activateListeners(html) {
    super.activateListeners(html);
  }
}