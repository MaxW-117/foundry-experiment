<form class="{{cssClass}}" autocomplete="off">
    <header class="sheet-header">
        <img class="profile-img" src="{{item.img}}" data-edit="img" title="{{item.name}}" />
        <div class="header-fields">
            <h1 class="charname"><input name="name" type="text" value="{{item.name}}" placeholder="Name" /></h1>
        </div>
    </header>
    <section class="grid grid-3col">
        <aside class="sidebar">
            <table>
                <tr>
                    <th>Attribute</th>
                    <th>#/level</th>
                </tr>
                {{#each item.displayStats as |stat|}}
                <tr>
                    <td title="{{stat.description}}">{{stat.name}}</td>
                    <td><input type="number" name="item.stats.{{stat.key}}" value="{{stat.value}}" data-dtype="Number"/></td>
                </tr>
                {{/each}}
            </table>
        </aside>

        <section class="main grid-span-2">

            {{editor content=data.description target="data.description" rollData=rollData button=true owner=owner
            editable=editable}}
        </section>

    </section>
</form>